# Brain Agriculture — Frontend

Vite + React 18 + TypeScript, Redux Toolkit, styled-components, React Router, Recharts. Testes com Jest + ts-jest + React Testing Library.

Documentação de como rodar, decisões e status de cada etapa vive no `README.md` da raiz do repositório (`../README.md#frontend`) — este arquivo só existe porque o Vite gera um por padrão; não duplica aquele conteúdo.
